snowflake:
  target: dev
  outputs:
    dev:
      type: bigquery
      project: "{{ env_var('BIGQUERY_PROJECT') }}"
      schema: "{{ env_var('DBT_SCHEMA') }}"
      method: oauth
      threads: 8
      location: EU